@model ProjetoFinalManuela.Models.Estado

@{
    ViewData["Title"] = "Details";
}

<h1>Detalhes</h1>

<div>
    <h4>Estado</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.EstadoName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.EstadoName)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.EstadoId" class="btn-principal">Editar</a> |
    <a asp-action="Index" class="btn-principal">Voltar à Lista</a>
</div>
